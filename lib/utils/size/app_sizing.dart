import 'package:flutter/material.dart';

const double kFigmaWidth = 390.0;
const double kFigmaHeight = 844.0;
// const double kFigmaWidth = 2436.0;
// const double kFigmaHeight = 1125.0;

double kScreenHeight(BuildContext context) {
  return MediaQuery.of(context).size.height;
}

double kScreenWidth(BuildContext context) {
  return MediaQuery.of(context).size.width;
}
